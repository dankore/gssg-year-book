<div class="max-w-sm sm:max-w-xs bg-white rounded shadow-lg">
  <div class="h-56">
    <% if(profile.photo){ %>
    <img
      class="w-full h-full object-cover"
      title="<%= profile.firstName %>"
      src="<%= profile.photo %>"
      alt="profile picture"
    />
    <% } else { %>
    <img
      class="w-full h-full object-cover"
      title="<%= profile.firstName %> <%= profile.lastName %>"
      src="../../static/user.svg"
      alt="profile picture"
    />
    <%}%>
  </div>
  <div class="px-6 py-4 relative">
    <div class="flex justify-end">
      <span
        class="absolute inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700"
        >SS3A</span
      >
    </div>
    <div
      class="font-bold mb-1 text-xl"
      style="overflow-wrap: break-word; min-width: 0px;"
    >
      <%= profile.firstName %> <%= profile.lastName %>
    </div>

    <p class="font-medium mb-1 font-mono text-center">
      👩🏽‍🎓 Class of <%= profile.year %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Nickname 👤:</span> <%= profile.nickname %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Lives in 🏡:</span> <%= profile.residence %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Occupation 👷‍♀:</span> <%= profile.occupation
      %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Birthday 📅:</span> <%= profile.month %> <%=
      profile.day %>
    </p>
  </div>
</div>
