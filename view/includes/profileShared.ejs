<div class="w-64 text-xs rounded overflow-hidden shadow-lg bg-white" style="height: 22rem">
    <% if(profile.photo){ %>
    <img
      class="w-full h-48 object-cover"
      title="<%= profile.firstName %>"
      src="<%= profile.photo %>"
      alt="profile picture"
    />
    <% } else { %>
    <img
      class="w-full h-48 object-cover"
      title="<%= profile.firstName %> <%= profile.lastName %>"
      src="../../static/blank.png"
      alt="profile picture"
    />
    <%}%>
  <div class="w-64 text-xs px-3 py-2 relative">
  <% if(profile.class){ %>
      <span
        class="absolute text-xs ml-48 inline-block bg-gray-200 rounded-full p-1 -mt-1 font-semibold text-gray-700">
        <%= profile.class %>
      </span
      >
  <% } else { %>
      <span
        class="absolute text-xs ml-48 inline-block bg-gray-200 rounded-full p-1 -mt-1 font-semibold text-gray-700">
        Class?
      </span
      >
  <%}%>
    <div
      class="font-bold text-md mb-1"
      style="overflow-wrap: break-word; min-width: 0px;"
    >
      <%= profile.firstName %> <%= profile.lastName %>
    </div>

    <p class="font-medium mb-1 font-mono text-center">
      👩🏽‍🎓 Class of <%= profile.year %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Nickname 👤:</span> <%= profile.nickname %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Lives in 🏡:</span> <%= profile.residence %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Occupation 👷‍♀:</span> <%= profile.occupation
      %>
    </p>
    <p class="mb-1">
      <span class="text-gray-600">Birthday 📅:</span> <%= profile.month %> <%=
      profile.day %>
    </p>
  </div>
</div>
