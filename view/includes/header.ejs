<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>GSS Gwarinpa Contact Book 📗</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <script src="/main-bundled.js" defer></script>
    <script src="https://unpkg.com/feather-icons"></script>
    <link type="text/css" href="/main.css" rel="stylesheet" />
  </head>

  <header class="bg-green-600 py-3 w-full font-bold text-white">
    <div class="flex justify-around items-center px-6">
      <a class="inline-block text-md sm:text-xl mr-2" href="/"
        >GSS Gwarinpa Contact Book
      </a>

      <% if (user) { %>
      <form action="/logout" method="POST" class="flex flex-wrap">
        <button
          class="mr-4 rounded border border-white hover:bg-green-800 px-3 py-1"
        >
          Logout
        </button>
      </form>

      <% } else { %>
      <div class="flex">
        <form action="/login" method="POST" class="flex flex-wrap">
          <div>
            <input
              name="email"
              type="text"
              class="shadow appearance-none border rounded py-1 px-3 mr-2 text-gray-700 leading-tight"
              placeholder="Email"
            />
          </div>
          <div class="relative">
            <input
              name="password"
              type="password"
              class="shadow appearance-none border rounded py-1 px-3 mr-2 text-gray-700 leading-tight"
              placeholder="Password"
            />
            <div>
              <a
                href="/reset-password"
                class="text-xs text-center absolute ml-16 mt-2 sm:mt-0 px-4"
                >Reset Your Password</a
              >
            </div>
            <button
              class="mr-4 rounded border border-white hover:bg-green-800 px-3 py-1"
            >
              Login
            </button>
          </div>
        </form>
        <a
          href="/register"
          class="rounded text-center flex items-center inline-block border border-white bg-blue-600
          hover:bg-blue-800 px-3 py-1"
        >
          Add Your Profile
        </a>
      </div>
    </div>
    <% } %>
  </header>

  <body class="bg-gray-200">
    <!-- BODY  -->
  </body>
</html>
